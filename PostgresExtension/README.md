./CPP/postgres_ext is the postgres extension, written in C (not C++) that acts as the middle-man between Postgres and the C++ "engine" for Hartonomous. This serves the same purpose between C++ and SQL/Postgres that a C# marshal wrapper project would for C#...