version: '3.8'

services:
  postgres-production:
    profiles:
      - production
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres_prod
      POSTGRES_DB: hartonomous_production
