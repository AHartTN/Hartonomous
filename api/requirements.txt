# FastAPI & ASGI server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.17

# PostgreSQL async driver (psycopg3)
psycopg[binary,pool]>=3.1.0

# Database migrations (Alembic)
alembic>=1.13.0
sqlalchemy>=2.0.0

# Neo4j Python driver (for provenance graph)
neo4j>=5.15.0

# Document parsing
pdfplumber>=0.11.0
python-docx>=1.2.0
markdown-it-py>=4.0.0
beautifulsoup4>=4.14.0

# Environment variables
python-dotenv>=1.0.0

# Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async utilities
aiofiles>=23.0.0
httpx>=0.26.0

# Retry logic
tenacity>=8.2.0

# NumPy/SciPy (matches in-database versions)
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0

# Image processing
pillow>=10.2.0

# Audio processing  
# wave (stdlib, no install needed)

# Compression (required by src/core/compression/multi_layer.py)
lz4>=4.3.2

# Semantic embeddings for geometric landmarks
sentence-transformers>=2.2.2

# Model format support
safetensors>=0.4.0  # SafeTensors format ingestion

# Parser dependencies (required by src/ingestion/parsers/)
pandas>=2.0.0  # structured_parser.py
tree-sitter>=0.20.0  # code_parser.py (optional but imported)

# Azure SDKs (production)
azure-identity>=1.15.0
azure-keyvault-secrets>=4.8.0
azure-appconfiguration>=1.5.0

# Entra ID / OAuth2
msal>=1.25.0
pyjwt[crypto]>=2.8.0
cryptography>=42.0.4

# ML/AI model parsing
gguf>=0.17.0
safetensors>=0.4.0

# Optional: Deep learning frameworks (for full model atomization)
# torch>=2.1.0  # PyTorch models (.pt, .pth)
# onnx>=1.15.0  # ONNX models (.onnx)

# Optional: GPU acceleration (uncomment for 5-10x speedup on weight deduplication)
# Install for your CUDA version:
#   CUDA 11.x: pip install cupy-cuda11x
#   CUDA 12.x+: pip install cupy
# Falls back to CPU if not available
cupy>=13.0.0

# Monitoring & logging
prometheus-client>=0.19.0
python-json-logger>=2.0.0

# Testing (dev dependencies)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.26.0  # for test client
